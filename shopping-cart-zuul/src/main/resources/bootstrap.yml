# Set Application Name
spring:
  application:
    name: shopping-cart-zuul
    
# Set Server port
server:
  port: ${PORT:9000}
  
# Information about Eureka Server
eureka:
  client:
    serviceUrl:
      defaultZone: http://127.0.0.1:8762/eureka/
